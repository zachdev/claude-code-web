# ── Optional overrides ────────────────────────────────────────────────────────
# Copy this file to .env and adjust as needed before running docker compose up.

# HTTP port exposed by the nginx reverse proxy (default: 80)
HTTP_PORT=80

# Timezone for all containers
TZ=UTC
